version: '3'
services:
  simple:
    build:
      context: .
      dockerfile: node.Dockerfile
    ports:
      - 4000:80
  route:
    build:
      context: .
      dockerfile: router.Dockerfile
    ports:
      - 3000:80
